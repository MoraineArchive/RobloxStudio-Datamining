# Initial Datamining Baseline

Type: baseline

This directory represents the initial public repository baseline.

It is not an upstream Roblox update. The timestamp below is the repository
initialization time, not a Roblox release time.

Studio version:
0.737.0.7371584

GUID:
version-9fe94fb0e9d84c25

Observed datasets:

- API
- Build
- FastVariables
- LiveSettings
- LuaPackages
- Native
- Plugins
- Web

The complete current state for each of these datasets lives under
[`current/`](../../../current/), and their exact source URLs and SHA-256
hashes are recorded in [`provenance.json`](provenance.json) in this same
directory.

No diff, findings, or changes are recorded here, because a baseline has no
prior state to compare against. The next real observed upstream transition
will create a dated event directory under `2026/` (or a later year) alongside
this one, containing `summary.md`, `event.json`, `diff.md`, `changes.json`,
`findings.md`, and `provenance.json`.
